export enum FilterOperators {
  all = '_all',
  has = '_has',
  eq = '_eq',
  in = '_in',
  like = '_like',
  contains = '_contains',
}

export enum FilterGameType {
  homePage = 'homePage',
  casino = 'casino',
  liveCasino = 'liveCasino',
  gameSearch = 'gameSearch',
  gameBanner = 'gameBanner',
  gamesPage = 'gamesPage',
  game = 'game',
}
